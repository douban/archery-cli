# archery-cli
archery cli to query, locate table


## openapi generate
在 archery 接口有更新的时候, 更新一下 openapi 的目录.
```shell
# 安装 nodejs
# 安装 npx
npm install -g npx

# 调用命令
npx @openapitools/openapi-generator-cli generate -c openapi-generator.yaml
```
